// console.log(1);
let form=document.querySelector("form");
let data={
    firstName:form.children[0].innerText,
    lastName:form.children[1].innerText,
    email:form.children[2].innerText,
    number:form.children[3].innerText,
    course:form.children[4].innerText,
};
